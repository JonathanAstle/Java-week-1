package com.qa.Interfaces;

//everything in an interface is abstract. Notice we don't have to specify that methods are abstract because they are by default
public interface FlyingCreature {
	public void fly();
}
